[workspace]
# Resolver v2 keeps feature unification scoped per dependency graph for workspace crates.
resolver = "2"
members = ["crates/aurora-nav-core"]

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.1.0"
