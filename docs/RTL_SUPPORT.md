# RTL Support

- Shared utility: `frontend/shared/rtl.ts`
- Supported RTL language roots: `he`, `ar`
- Apps derive directionality through `computeDirection()` and can map output to `dir` in UI shells.
